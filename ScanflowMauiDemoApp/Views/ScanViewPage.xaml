<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ScanflowMauiDemoApp.Views.ScanViewPage"
             Title="ScanViewPage"
             xmlns:controls="clr-namespace:Scanflow.BarcodeCapture.Maui;assembly=Scanflow.BarcodeCapture.Maui">

    <NavigationPage.TitleView>
        <Grid>
            <Label x:Name="scanTitle" TextColor="White" HorizontalOptions="StartAndExpand" VerticalOptions="Center" FontSize="17" Margin="0,15,18,20"/>
            <Image Source="settings.svg" VerticalOptions="Center" HorizontalOptions="EndAndExpand" HeightRequest="28" Margin="{OnPlatform Android='0,15,18,20',iOS='0'}">
                <Image.GestureRecognizers>
                    <TapGestureRecognizer Tapped="Setting_Tapped" />
                </Image.GestureRecognizers>
            </Image>
        </Grid>

    </NavigationPage.TitleView>

    <ContentPage.Content>
        <Grid>
            <Grid x:Name="backdrop" >

                <controls:CameraPreview  x:Name="barcodeCaptureScan" OnScanResult="barcodeCaptureScan_OnScanResult" OnLicenceOnSuccessWithResponse="BarcodeCaptureScan_OnLicenceOnSuccessWithResponse" OnLicenceOnFailureWithError="BarcodeCaptureScan_OnLicenceOnFailureWithError" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand"/>

                <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                    <ActivityIndicator IsVisible="false" Opacity="1" x:Name="progressIndicator" IsRunning="False" IsEnabled="False"   Color="White" HorizontalOptions="End" VerticalOptions="EndAndExpand" Margin="15,30,85,37" HeightRequest="25">
                    </ActivityIndicator>
                    <Button Text="Validate Key" IsVisible="true" Clicked="btnRetryValidation" BackgroundColor="#0C54C5" TextColor="White" VerticalOptions="EndAndExpand" Margin="25"
                      />
                </StackLayout>


                <Image Source="flashon" x:Name="torchImage" VerticalOptions="StartAndExpand" HorizontalOptions="EndAndExpand" HeightRequest="30" Margin="0,20,20,0">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="FlashLight_Tapped"/>
                    </Image.GestureRecognizers>
                </Image>



            </Grid>
        </Grid>
    </ContentPage.Content>

</ContentPage>